# Processing log: schools-map-mathex

Дата генерации: 2026-06-07 (итерация 2 — добавлены Forbes-школы, цвета, сокращения, сайты)  
Источники: 
- https://mathex.ru/shkoly/ (список лучших школ Москвы + раздел "Другие топ школы")
- https://www.forbes.ru/ (рейтинг частных школ 2025 года, 3 ценовых сегмента)

Геокодер: Яндекс.Карты HTTP Geocoder API (`geocode-maps.yandex.ru/1.x/`)

## 1. Сводные числа

| Показатель | Значение |
|---|---|
| Всего школ в финальной базе | **69** |
| Из Mathex (12 основных + 6 дополнительных) | 18 |
| Из Forbes (LOW сегмент) | 9 |
| Из Forbes (MID сегмент) | 23 |
| Из Forbes (HIGH сегмент) | 19 |
| Успешно геокодировано | 69 |
| Проблемных школ | 0 |

## 2. Геокодирование

Все 69 школ геокодированы через Яндекс.Геокодер, precision=exact.

## 3. Дубли

Явных дубликатов по паре (name, address) нет.  
Кросс-источниковая дедупликация: школы "Летово" и "Школа ЦПМ" оставлены из Mathex (полнее), данные Forbes перенесены в `notes`.

## 4. Цветовая маркировка маркеров (GeoJSON → Яндекс.Карты)

Цвет задаётся через свойство `properties.marker-color` (стандарт geojson.io, который Я.Карты читают при импорте GeoJSON):

| Сегмент | Кол-во | marker-color | Ценовой диапазон |
|---|---|---|---|
| Mathex (без ценовой категории) | 18 | `#1e98ff` (синий) | муниципальные / бесплатные |
| Forbes LOW (низкий) | 9 | `#ffd21e` (жёлтый) | до 580 000 руб/год |
| Forbes MID (средний) | 23 | `#ff7800` (оранжевый) | 580 000 — 1 300 000 руб/год |
| Forbes HIGH (высокий) | 19 | `#ef3c4a` (красный) | от 1 300 000 руб/год |

**Как сделать все метки круглыми в Яндекс.Конструкторе:**  
После импорта `schools.geojson` в Конструктор → открыть настройки слоя → "Стиль значка" → выбрать "Круглый значок" (вместо "С флажком" по умолчанию). Это делается в UI один раз для всего слоя, форма задаётся не через GeoJSON.

## 5. Сокращение подписей (iconCaption)

Все подписи на метках сокращены до ≤ 9 символов. Примеры:
- "Школа 1533 ЛИТ" → "1533 ЛИТ"
- "Школа 1580" → "1580"
- "Школа Интеллектуал" → "Интел."
- "Лицей Вторая школа" → "Л2Ш"
- "Лицей Воробьевы Горы" → "Воробёвы"
- "Гимназия Святителя Василия Великого" → "ГСВВ"
- "Британская международная школа" → "Brit.Int."
- "Силаэдр" → "Силаэдр"

В карточке (description) сохраняется полное имя как первая строка — пользователь видит полное название при клике на метку.

## 6. Структура description (карточки)

Формат (по образцу карточки "Силаэдр" из предыдущей итерации):
```
{Полное имя}
                     ← пустая строка (визуальный разделитель)
{Адрес}            ← БЕЗ жирного
{Цена руб/год}     ← только для Forbes-школ
Сайт: <a href>...</a>      ← кликабельная ссылка (если есть)
Страница на Mathex   ← кликабельная ссылка (только для Mathex-школ)
```

Убраны лишние поля: "Сегмент", "Основана", "Приоритетные ценности".

## 7. Структура итоговых файлов

| Файл | Назначение | Размер |
|---|---|---|
| `schools_raw.csv` | Сырой слой: 24 поля × 69 строк | ~352 KB |
| `schools_normalized.csv` | Нормализованный слой: 11 полей × 69 | ~33 KB |
| `schools_import_yandex.csv` | Для ручного импорта в Я.Конструктор: `lat;lon;description;label;number` | ~18 KB |
| `schools.geojson` | **Основной формат**: FeatureCollection × 69 Point-фич, marker-color по сегменту, HTML-description с кликабельными ссылками, iconCaption ≤ 9 символов | ~36 KB |
| `schools.xlsx` | Excel: 2 листа — `normalized`, `yandex_import` | ~21 KB |
| `schools_failed_geocoding.csv` | Пустой (все успешно) | ~0.2 KB |

## 8. Поток обработки (итерация 2)

1. Из исходного `schools.geojson_07-06-2026_21-17-59.geojson` (скачан пользователем) извлечён рабочий формат: `description` с `
` (первая строка = заголовок карточки), `iconCaption` для подписи, `marker-color` для цвета.
2. Сокращены подписи до ≤ 9 символов (словарь SHORTEN_MAP).
3. Перестроен `description`: убран `<b>` с адресов, убраны поля "Сегмент", "Основана", "Приоритетные ценности", добавлены ссылки на сайты Forbes-школ.
4. Применены `marker-color`:
   - Mathex → `#1e98ff` (синий)
   - Forbes low → `#ffd21e` (жёлтый)
   - Forbes mid → `#ff7800` (оранжевый)
   - Forbes high → `#ef3c4a` (красный)
5. Обновлены `schools_normalized.csv`, `schools_import_yandex.csv`, `schools.xlsx`, `schools.geojson`.

## 9. Заметки по ручному расширению

- Добавить новую школу: редактировать `schools_normalized.csv` (или лист `normalized` в `schools.xlsx`), затем перегенерировать geojson: `cd /opt/data/schools-map-mathex && source .venv/bin/activate && python3 ...` (скрипт ниже).
- Форму меток (круглые/флажки) задавать в UI Я.Конструктора → стиль слоя.
- Цвет: менять `properties.marker-color` в geojson перед импортом.
- Школы "Проект 2*2" и "Воробьевы Горы маткласс на Донской" не включены — у Mathex нет адресов, нужны ручной сбор.
